# [需求名称] — 测试报告（TE 产出）

## 导航头
- R/S 覆盖：R-xxx、S-xxx
- 文档状态：Draft / Updated

> 写入纪律：完全重写，禁止占位符残留；最后一个 `## 结论` 之后不得再出现任何二次模板标题。
> 红线：B 类未在真实浏览器执行不得 PASS（缺环境就写 FAIL（环境阻塞））。

## A. API 测试

（A-1 功能正确性 / A-2 权限控制 / A-3 数据校验；表格：编号 / 场景 / 前置 / 预期 / 实际 / 判定）

## B. 功能验收测试（Given-When-Then）

（B-1 页面与交互 / B-2 边界与异常；每条正向 S-xxx ≥1 + 每条异常/边界 S-xxx ≥1，合计 ≥6；
表格：编号 / R / S / Given / When / Then / 实际 / 判定）

### E2E 文本用例 ↔ Playwright 对照表

（必填，与 `e2e/<任务>.e2e.js` 的 test() 1:1；表格：编号 / GWT 摘要 / Playwright test() / 证据 / 判定）

## C. 回归测试

（改动影响面内最小相关集：API 探测 + 选择性 1-2 个相关历史 spec）

## D. 工程验证

- build-test：（npm run test:all + 后端启动 + 前端构建 + 种子；贴关键输出）
- post-verify：（verify.sh + baseline compare；贴关键输出）

## 统计汇总

| 类别 | 用例数 | 通过 | 失败 | 通过率 |
|---|---|---|---|---|
| 合计 | 通过: X 项 | | | |

## 失败项详情（有 FAIL 时必填）

（每条：复现命令/步骤 / 原始证据 / 失败边界定位 / 最小影响面 / 严重程度）

## 结论

PASS / FAIL（FAIL 子类型括号注明：功能 bug / 需求级 / 环境阻塞 / 定位阻塞）
